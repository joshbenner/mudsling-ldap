[ldap]
server = localhost
port = 389
base dn = dc=example,dc=com
bind dn = cn=admin,dc=example,dc=com
bind pw = password
user base dn = ou=users,dc=example,dc=com
unique attr = entryUUID
password attr = userPassword
username attr = cn
timeout = 5
tls = no
